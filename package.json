{
  "name": "@mosteast/common_eid",
  "version": "0.2.0",
  "description": "Common error id for mosteast project (Only for internal usage).",
  "homepage": "https://github.com/mosteast/common_eid#readme",
  "repository": "github:mosteast/common_eid",
  "license": "ISC",
  "author": "Mosteast team",
  "types": "./index.d.ts",
  "files": [
    "build/"
  ],
  "scripts": {
    "build": "./bin/build",
    "build_and_release": "npm run build && cd build && npm run release",
    "format": "npx sort-package-json && npx prettier --write .",
    "release": "npm publish --access public",
    "test": "npx jest --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/fs-extra": "^8.0.1",
    "@types/jest": "^24.0.23",
    "@types/node": "^11.15.3",
    "@types/shelljs": "^0.8.6",
    "fs-extra": "^8.1.0",
    "jest": "^24.9.0",
    "shelljs": "^0.8.3",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.5.4",
    "typescript": "^3.7.2"
  }
}
